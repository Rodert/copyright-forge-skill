# Consistency Rules

Every artifact must use the profile's exact software full name and version.
Features named in the document must have evidence-map entries. The validation
report is `READY` only when no blockers remain and all user-only required facts
are confirmed.
